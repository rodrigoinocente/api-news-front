import styled from "styled-components";

export const TimeStyled = styled.section`
    color: #3a3939;
    font-size: .8rem;

    .full {
        display: flex;
        flex-direction: row;
        gap: .4rem;
    }
`