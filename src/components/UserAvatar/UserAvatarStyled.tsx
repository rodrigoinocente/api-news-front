import styled from "styled-components";

export const UserAvatarContiner = styled.div`
    img {
        width: 2rem;
        aspect-ratio: 1/1;
        border-radius: 50%;
        object-fit: cover;
        cursor: pointer;
    }

`